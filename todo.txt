split the line by whitespace so lines dont need to be exactly spaced
update argument error checking to make sure the arguments are also correct (reg is reg number, imm is number of if applicable fn name)

